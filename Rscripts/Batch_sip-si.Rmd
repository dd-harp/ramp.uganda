---
title: "Batch_sip-si"
output: html_document
---

```{r}
library(ramp.xds)
library(ramp.library)
library(ramp.control)
library(ramp.work)
library(ramp.uganda)
devtools::load_all()
```

```{r}
base_mod = "sip_si" 
wd = "/Users/smitdave/Library/CloudStorage/Box-Box/RAMP/models/"
base_mod <- "sip_si"
save_as <- paste(base_mod, ".rds", sep="")
file_name <- paste(wd, save_as, sep="")
file_name
```

```{r}
sip_si <- xds_setup(MYZname = "SI", Xname = "SIP")
sip_si <- xde_scaling_lambda(sip_si)
```

```{r}
saveRDS(sip_si, file_name)
```

```{r}
pipeline(13, base_mod, wd)
```
These errors: 

```{r}
devtools::load_all()
```

```{r}
pdf("~/git/ramp.uganda/models/sip_si.pdf")
for(i in 1:146) viz_pipe_1(i, base_mod, wd, pdf=FALSE)
dev.off(dev.cur())
```

```{r}
for(i in 1:146) viz_pipe(i, base_mod, wd, pdf=TRUE)
```

```{r}
i=1
viz_pipe(133, base_mod, wd) 
```


